import { default as Compiler } from '../../compiler';
import { CodeActionProvider, CodeActionList, CodeActionContext, TextModel, Range, CancellationToken } from '../types';
export default class DBMLCodeActionProvider implements CodeActionProvider {
    private compiler;
    constructor(compiler: Compiler);
    provideCodeActions(model: TextModel, _range: Range, context: CodeActionContext, _token: CancellationToken): CodeActionList | undefined;
    private quickFixToCodeAction;
}
