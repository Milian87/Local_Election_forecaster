import { CompileError } from '../../../types/errors';
import { Dep } from '../../../types/schemaJson';
import { NodeMetadata } from '../../../types/symbol/metadata';
export declare function validateDepBlocks(dep: Dep, meta: NodeMetadata): CompileError[];
