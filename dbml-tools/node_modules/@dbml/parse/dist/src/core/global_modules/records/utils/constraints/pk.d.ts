import { default as Compiler } from '../../../../../compiler/index';
import { CompileInfo } from '../../../../types/errors';
import { SyntaxNode } from '../../../../types/nodes';
import { TableRecord } from '../../../../types/schemaJson';
import { TableSymbol } from '../../../../types/symbol';
export declare function validatePrimaryKey(compiler: Compiler, tableSymbol: TableSymbol, recordBlock: SyntaxNode, record: TableRecord): CompileInfo[];
