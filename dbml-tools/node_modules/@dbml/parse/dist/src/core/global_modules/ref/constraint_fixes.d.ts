import { default as Compiler } from '../../../compiler';
import { CompileInfo } from '../../types/errors';
import { RefMetadata, PartialRefMetadata } from '../../types/symbol/metadata';
export declare function validateCardinality(compiler: Compiler, meta: RefMetadata | PartialRefMetadata, side: 'left' | 'right', // Whether we're validating the left side or the right side
options?: {
    allowOtherColFix?: boolean;
    allowOwnColFix?: boolean;
}): CompileInfo[];
export declare function validatePartialRef(compiler: Compiler, meta: PartialRefMetadata): CompileInfo[];
