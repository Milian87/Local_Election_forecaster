import { default as Compiler } from '../../../compiler';
import { Filepath } from '../../types';
import { MetadataElementMetadata } from '../../types/symbol/metadata';
import { default as Report } from '../../types/report';
import { MetadataValues } from '../../types/schemaJson';
export default class MetadataInterpreter {
    private declarationNode;
    private compiler;
    private filepath;
    private values;
    constructor(compiler: Compiler, metadata: MetadataElementMetadata, filepath: Filepath);
    interpret(): Report<MetadataValues>;
    private interpretValues;
}
