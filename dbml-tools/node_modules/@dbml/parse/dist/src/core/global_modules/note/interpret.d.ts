import { Note } from '../../types/schemaJson';
import { default as Compiler } from '../../../compiler';
import { SettingName, Filepath, NoteSymbol } from '../../types';
import { default as Report } from '../../types/report';
import { MetadataFieldRegistry } from '../metadata/utils';
export declare const NOTE_METADATA_FIELDS: MetadataFieldRegistry<Note, SettingName.Color>;
export declare class StickyNoteInterpreter {
    private declarationNode;
    private symbol;
    private compiler;
    private filepath;
    private note;
    constructor(compiler: Compiler, symbol: NoteSymbol, filepath: Filepath);
    interpret(): Report<Note>;
    private interpretName;
    private interpretSettingList;
    private interpretBody;
    private interpretNote;
}
