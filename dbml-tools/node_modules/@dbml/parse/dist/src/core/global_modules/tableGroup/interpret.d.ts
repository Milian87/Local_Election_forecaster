import { TableGroup } from '../../types/schemaJson';
import { default as Compiler } from '../../../compiler';
import { SettingName, Filepath, TableGroupSymbol } from '../../types';
import { default as Report } from '../../types/report';
import { MetadataFieldRegistry } from '../metadata/utils';
export declare const TABLEGROUP_METADATA_FIELDS: MetadataFieldRegistry<TableGroup, SettingName.Note | SettingName.Color>;
export declare class TableGroupInterpreter {
    private compiler;
    private symbol;
    private declarationNode;
    private filepath;
    private tableGroup;
    constructor(compiler: Compiler, symbol: TableGroupSymbol, filepath: Filepath);
    interpret(): Report<TableGroup>;
    private interpretName;
    private interpretBody;
    private interpretSubElements;
    private interpretFields;
    private interpretSettingList;
}
