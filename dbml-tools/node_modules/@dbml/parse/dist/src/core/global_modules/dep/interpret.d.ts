import { Dep } from '../../types/schemaJson';
import { DepMetadata, Filepath } from '../../types';
import { default as Compiler } from '../../../compiler';
import { default as Report } from '../../types/report';
export declare class DepInterpreter {
    private compiler;
    private metadata;
    private declarationNode;
    private filepath;
    private dep;
    constructor(compiler: Compiler, metadata: DepMetadata, filepath: Filepath);
    interpret(): Report<Dep>;
    private interpretName;
    private interpretEdges;
    private interpretSettings;
    private consumeSettings;
    private extractAttrValue;
}
