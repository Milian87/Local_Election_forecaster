import { default as Compiler } from '../../../compiler';
import { CompileInfo, QuickFix } from '../../types/errors';
import { SyntaxNode } from '../../types/nodes';
import { SyntaxToken } from '../../types/tokens';
import { ColumnSymbol } from '../../types/symbol';
import { RelationshipOp } from '../../types/relation';
export declare function refNullableMismatch(compiler: Compiler, node: SyntaxNode | SyntaxToken, options: {
    relOp: RelationshipOp;
    columns: ColumnSymbol[];
    refNode: SyntaxNode;
    quickFixes?: QuickFix[];
}): CompileInfo;
export declare function refNotNullMismatch(compiler: Compiler, node: SyntaxNode | SyntaxToken, options: {
    relOp: RelationshipOp;
    columns: ColumnSymbol[];
    refNode: SyntaxNode;
    quickFixes?: QuickFix[];
}): CompileInfo;
export declare function refUniqueMismatch(compiler: Compiler, node: SyntaxNode | SyntaxToken, options: {
    relOp: RelationshipOp;
    columns: ColumnSymbol[];
    refNode: SyntaxNode;
    quickFixes?: QuickFix[];
}): CompileInfo;
export declare function refNonUniqueMismatch(compiler: Compiler, node: SyntaxNode | SyntaxToken, options: {
    relOp: RelationshipOp;
    columns: ColumnSymbol[];
    refNode: SyntaxNode;
    quickFixes?: QuickFix[];
}): CompileInfo;
