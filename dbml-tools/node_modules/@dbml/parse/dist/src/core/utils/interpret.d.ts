import { default as Compiler } from '../../compiler';
import { Color, ColumnType, CustomMetadata, TokenPosition } from '../types/schemaJson';
import { SyntaxNode } from '../types/nodes';
import { default as Report } from '../types/report';
import { Settings } from './validate';
import { SettingName } from '../types';
export declare function getTokenPosition(node: SyntaxNode): TokenPosition;
export declare function normalizeNote(content: string): string;
export declare function extractElementName(nameNode: SyntaxNode): {
    schemaName: string[];
    name: string;
};
export declare function extractColor(node?: SyntaxNode): Color | undefined;
export declare function processDefaultValue(valueNode?: SyntaxNode): {
    type: 'number' | 'string' | 'boolean' | 'expression';
    value: string | number;
} | undefined;
export declare function processColumnType(compiler: Compiler, typeNode: SyntaxNode): Report<ColumnType>;
/**
  * Extract inline custom metadata from an aggregated setting list,
  * excluding keys that are natively attached to the element (headercolor and note in `table`, color in `tablegroup`, .etc).
  *
  * NOTE: This is used during interpret phase, so no need to validate values, just extract them
  */
export declare function extractCustomInlineMetadata(settingMap: Settings, builtinSettings: readonly SettingName[]): CustomMetadata;
export declare function extractMetadataValue(node?: SyntaxNode): string | Color | undefined;
