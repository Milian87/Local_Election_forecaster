import { SyntaxNode } from '../types/nodes';
import { TextEdit } from '../../compiler/queries/transform/applyTextEdits';
/**
 * Produces a TextEdit that updates an existing note's value.
 * Returns undefined if no note exists.
 */
export declare function updateNoteEdit(declaration: SyntaxNode, newValue: string): TextEdit | undefined;
/**
 * Produces a TextEdit that removes an existing note.
 * Returns undefined if no note exists.
 */
export declare function removeNoteEdit(declaration: SyntaxNode): TextEdit | undefined;
/**
 * Produces a TextEdit that adds a note to an element.
 * Prioritizes body note form for block elements, falls back to setting attribute.
 * Returns undefined if a note already exists.
 */
export declare function addNoteEdit(declaration: SyntaxNode, value: string): TextEdit | undefined;
