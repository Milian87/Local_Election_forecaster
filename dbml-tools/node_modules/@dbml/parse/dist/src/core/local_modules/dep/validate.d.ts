import { default as Compiler } from '../../../compiler';
import { CompileError } from '../../types/errors';
import { BlockExpressionNode, ElementDeclarationNode, FunctionApplicationNode, ListExpressionNode } from '../../types/nodes';
import { default as Report } from '../../types/report';
import { Settings } from '../../utils/validate';
export default class DepValidator {
    private declarationNode;
    private compiler;
    constructor(compiler: Compiler, declarationNode: ElementDeclarationNode);
    validate(): CompileError[];
    private validateSettings;
    private validateContext;
    private validateName;
    private validateAlias;
    validateBody(body?: FunctionApplicationNode | BlockExpressionNode): CompileError[];
    validateFields(fields: FunctionApplicationNode[]): CompileError[];
    private validateSubElements;
}
export declare function validateDepSettings(settings: ListExpressionNode): Report<Settings>;
