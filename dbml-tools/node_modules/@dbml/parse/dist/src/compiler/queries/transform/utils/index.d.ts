import { Filepath } from '../../../../core/types/filepath';
import { NodeSymbol, SymbolKind } from '../../../../core/types/symbol';
import { default as Compiler } from '../../../index';
import { EndpointRef } from '../types';
export type { ElementIdentifier } from '../types';
export { findRefDefinition, type InlineRef, type StandaloneRef } from './ref';
export { findDepDefinition, type DepDefinition, type InlineDepDefinition, type BlockDepDefinition, } from './dep';
export declare function normalizeSchema(schema?: string | null): string;
export declare function normalizeTableName(input: string | {
    schema?: string;
    table: string;
}): {
    schema: string;
    table: string;
};
/**
 * Looks up an element symbol by its qualified name and kind.
 */
export declare function lookupElementSymbol(compiler: Compiler, filepath: Filepath, schema: string, name: string, kind?: SymbolKind): NodeSymbol | undefined;
/**
 * Removes surrounding double quotes from a string if present.
 */
export declare function stripQuotes(str: string): string;
export declare function formatEndpoint(ep: EndpointRef): string;
export declare function formatSetting(name: string, value: string | null | undefined): string;
