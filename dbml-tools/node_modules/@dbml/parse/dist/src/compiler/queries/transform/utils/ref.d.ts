import { FunctionApplicationNode } from '../../../../core/types/nodes';
import { Filepath } from '../../../../core/types/filepath';
import { default as Compiler } from '../../../index';
import { RefIdentifier } from '../types';
export interface InlineRef {
    kind: 'inline';
    op: string;
    fullStart: number;
    fullEnd: number;
}
export interface StandaloneRef {
    kind: 'standalone';
    node: FunctionApplicationNode;
}
export declare function findRefDefinition(compiler: Compiler, filepath: Filepath, target: RefIdentifier): InlineRef | StandaloneRef | undefined;
